name: quarkus-jnosql-elasticsearch-codestart
ref: quarkus-jnosql-elasticsearch
type: code
tags:
  - extension-codestart
  - maven-only
metadata:
  title: Quarkus JNoSQL Elasticsearch Codestart
  description: Create your first Document NoSQL entity with Quarkus and JNoSQL Elasticsearch.
  related-guide-section: https://docs.quarkiverse.io/quarkus-jnosql/dev/
language:
  base:
    data:
      database-type: "document"
      database-name: "garage"
    test-dependencies:
      - groupId: org.testcontainers
        artifactId: testcontainers
        scope: test

      - groupId: io.quarkus
        artifactId: quarkus-junit4-mock
        scope: test

      - groupId: net.datafaker
        artifactId: datafaker
        version: ${datafaker.version}
        scope: test

      - groupId: org.assertj
        artifactId: assertj-core
        version: ${assertj.version}
        scope: test