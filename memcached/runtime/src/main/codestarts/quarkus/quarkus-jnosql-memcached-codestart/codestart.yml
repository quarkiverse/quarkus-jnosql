name: quarkus-jnosql-memcached-codestart
ref: quarkus-jnosql-memcached
type: code
tags:
  - extension-codestart
  - maven-only
metadata:
  title: Quarkus JNoSQL Memcached Codestart
  description: Create your first Key-Value NoSQL entity with Quarkus and JNoSQL Memcached.
  related-guide-section: https://docs.quarkiverse.io/quarkus-jnosql/dev/
language:
  base:
    test-dependencies:
        - groupId: org.testcontainers
          artifactId: testcontainers
          scope: test

        - groupId: io.quarkus
          artifactId: quarkus-junit-mockito
          scope: test

        - groupId: net.datafaker
          artifactId: datafaker
          version: ${datafaker.version}
          scope: test

        - groupId: org.assertj
          artifactId: assertj-core
          version: ${assertj.version}
          scope: test
