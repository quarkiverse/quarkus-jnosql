name: quarkus-jnosql-mongodb-codestart
ref: quarkus-jnosql-mongodb
type: code
tags:
  - extension-codestart
  - maven-only
metadata:
  title: Quarkus JNoSQL MongoDB Codestart
  description: Create your first Document NoSQL entity with Quarkus and JNoSQL MongoDB.
  related-guide-section: https://docs.quarkiverse.io/quarkus-jnosql/dev/
language:
  base:
    data:
      database-name: "garage"
    test-dependencies:

        - groupId: net.datafaker
          artifactId: datafaker
          version: ${datafaker.version}
          scope: test

        - groupId: org.assertj
          artifactId: assertj-core
          version: ${assertj.version}
          scope: test